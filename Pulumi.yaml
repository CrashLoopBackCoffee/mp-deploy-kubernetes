name: deploy-kubernetes
description: Kubernetes stack on Proxmox cluster
pulumi:disable-default-providers:
  - kubernetes
  - proxmoxve
runtime:
  name: python
  options:
    toolchain: poetry
    virtualenv: .venv
